<!-- ORGANIZER

- the error text is appearing below the total guests count when adding manual guest
- for managing guests, add a checkbox for each row and add a bulk delete button to delete multiple guests at once
- remove edit button and just add an info about you cannot change this later when adding a manual guest
- add a confirmation pop-up when deleting a guest
- add a confirmation pop-up when deleting multiple guests at once -->